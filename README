CAS4109 과제 #3 - Part E

이 프로그램은 OpenSSL을 사용하여 파일의 암호화/복호화를 수행하는 Rust 프로그램입니다.

구현 세부사항:
- AES-256-CBC를 사용한 대칭키 암호화
- HMAC-SHA256을 사용한 메시지 인증
- 암호화된 데이터와 HMAC 태그는 별도의 파일로 저장

사용법:
1. 암호화: ./cryp enc -key <키파일> -in <입력파일> -out <출력파일> -tag <태그파일>
2. 복호화: ./cryp dec -key <키파일> -in <암호화된파일> -tag <태그파일> -out <출력파일>

오류 코드:
- 0: 성공
- 1: HMAC 검증 실패
- 2: 기타 오류 (파일 접근 오류, 잘못된 인자 등)
